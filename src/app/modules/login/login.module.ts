import { NgModule } from '@angular/core';

import { LoginRoutingModule, LoginComponents } from './login-routing.module';

@NgModule({
    imports: [
        LoginRoutingModule
    ],
    declarations: [
        LoginComponents
    ]
})

export class LoginModule {
    
}